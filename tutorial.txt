<-----------index.js file code explanation ----->
step bt step explanation 

1------
import express from "express";
import cors from "cors";
import multer from "multer";
import dotenv from "dotenv";
import { Queue } from "bullmq";
import OpenAI from "openai";
import { OpenAIEmbeddings } from "@langchain/openai";
import { QdrantVectorStore } from "@langchain/qdrant";
এখানে প্রয়োজনীয় প্যাকেজগুলো ইম্পোর্ট করা হয়েছে:

2------
express → সার্ভার বানানোর জন্য
cors → ফ্রন্টএন্ড থেকে API কল করার জন্য
multer → ফাইল (PDF) আপলোড করার জন্য
dotenv → .env ফাইল থেকে API Key/Config নেয়ার জন্য
bullmq → Redis-ভিত্তিক Queue সিস্টেম
openai → OpenAI এর API ব্যবহার করার জন্য
langchain এর OpenAIEmbeddings + QdrantVectorStore → টেক্সটকে ভেক্টর এ রূপান্তর করে Qdrant DB তে স্টোর করার জন্য

3------
const client = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});
👉 OpenAI ক্লায়েন্ট ইনিশিয়ালাইজ করা হলো।

4-----
const queue = new Queue("file-upload-queue", {
  connection: {
    host: "localhost",
    port: 6379,
  },
});
👉 BullMQ Queue বানানো হলো, যেখানে Redis চলবে localhost:6379 এ।
মানে — ফাইল আপলোড হলে Queue তে কাজ পাঠানো হবে, আর Worker সেটা প্রসেস করবে।

5-----
const storage = multer.diskStorage({
  destination: function (req, file, cb) {
    cb(null, "uploads/");
  },
  filename: function (req, file, cb) {
    const uniqueSuffix = Date.now() + "-" + Math.round(Math.random() * 1e9);
    cb(null, `${uniqueSuffix}-${file.originalname}`);
  },
});
👉 Multer দিয়ে ফাইল লোকাল ফোল্ডারে (uploads/) সেভ হচ্ছে।
প্রতিটা ফাইলের নামের আগে একটা ইউনিক timestamp + random number যোগ করা হচ্ছে।

6-----
const upload = multer({ storage: storage });
const app = express();
app.use(cors());
👉 upload middleware তৈরি হলো।
👉 Express app বানানো হলো এবং CORS allow করা হলো।

7------
app.post("/upload/pdf", upload.single("pdf"), async (req, res) => {
  await queue.add(
    "file-ready",
    JSON.stringify({
      fileName: req.file.originalname,
      destination: req.file.destination,
      path: req.file.path,
    })
  );
  return res.json({ message: "File uploaded successfully!" });
});
যখন /upload/pdf এ POST করা হবে:
ফাইল uploads/ ফোল্ডারে সেভ হবে।
Queue তে জব পাঠানো হবে (Worker পরে সেটা প্রসেস করবে)।
Response যাবে "File uploaded successfully!"


8----
app.get("/chat", async (req, res) => {
  const userQuery = req.query.message;
  console.log("userQuery:", userQuery);

  const embeddings = new OpenAIEmbeddings({
    model: "text-embedding-3-small",
    apiKey: process.env.OPENAI_API_KEY,
  });
  const vectorStore = await QdrantVectorStore.fromExistingCollection(
    embeddings,
    {
      url: "http://localhost:6333",
      collectionName: "langchainjs-testing",
    }
  );

  const ret = vectorStore.asRetriever({ k: 2 });

  👉 এখানে vectorStore থেকে একটা retriever বানানো হচ্ছে।
retriever এর কাজ হলো: ইউজারের প্রশ্ন অনুযায়ী Qdrant ডাটাবেস থেকে সবচেয়ে relevant (মিল আছে এমন) ডকুমেন্ট খুঁজে বের করা।
{ k: 2 } মানে → ২টা সবচেয়ে কাছাকাছি match করা ডকুমেন্ট ফেরত দেবে।


  const result = await ret.invoke(userQuery);
  👉 এখানে retriever কে বলা হচ্ছে → "এই ইউজারের query (প্রশ্ন) অনুযায়ী মিলে যাওয়া ডকুমেন্ট বের করো"।
userQuery হলো ইউজারের দেওয়া প্রশ্ন।
result এ থাকবে ঐ প্রশ্নের সাথে মিলে যাওয়া top 2 PDF টুকরা (chunk) যেগুলো embeddings এর উপর ভিত্তি করে Qdrant থেকে ফেচ করা হয়েছে।

  const SYSTEM_PROMPT = `
  You are helfull AI Assistant who answeres the user query based on the available context from PDF File.
  Context:
  ${JSON.stringify(result)}
  `;

  const chatResult = await client.chat.completions.create({
    model: "gpt-4.1",
    messages: [
      { role: "system", content: SYSTEM_PROMPT },
      { role: "user", content: userQuery },
    ],
  });

👉 এখানে client.chat.completions.create ব্যবহার করা হচ্ছে।
মানে → OpenAI এর Chat Completion API কে কল করা হচ্ছে।
model: "gpt-4.1" → এখানে কোন মডেল ব্যবহার করতে হবে সেটি বলা হয়েছে (GPT-4.1)।
messages → এটা একটা মেসেজ লিস্ট (কনভারসেশন হিস্ট্রি)।
{ role: "system", content: SYSTEM_PROMPT } → System Prompt দেওয়া হয়েছে।
এতে retriever থেকে পাওয়া context রাখা আছে (PDF এর অংশবিশেষ), যাতে AI সেই অনুযায়ী উত্তর দেয়।
{ role: "user", content: userQuery } → এখানে ইউজারের আসল প্রশ্ন পাঠানো হয়েছে।

📌 কাজের ধাপ:
AI কে প্রথমে বলা হলো → "তুমি একজন হেল্পফুল অ্যাসিস্ট্যান্ট, আর এই context অনুযায়ী ইউজারের প্রশ্নের উত্তর দেবে।"
তারপর ইউজারের আসল প্রশ্ন পাঠানো হলো।
👉 ফলে AI উত্তর তৈরি করবে এবং সেটা chatResult এ চলে আসবে।

  return res.json({
    message: chatResult.choices[0].message.content,
    docs: result,
  });
});
👉 এখানে ক্লায়েন্টের কাছে (ফ্রন্টএন্ডে) JSON রেসপন্স পাঠানো হচ্ছে।
message → AI যে উত্তর দিয়েছে (chatResult.choices[0].message.content) সেটা রাখা হলো।
docs → retriever থেকে আনা PDF এর relevant অংশ (context) একসাথে ফেরত দেওয়া হলো।


👉 /chat রুট:
ইউজারের প্রশ্ন নেয়া হলো req.query.message থেকে।
Qdrant DB থেকে ভেক্টর সার্চ করা হলো (PDF context থেকে)।
System Prompt বানানো হলো যেখানে Context inject করা হলো।
OpenAI Chat API কে কল করা হলো (gpt-4.1 মডেল দিয়ে)।
Response JSON আকারে ফেরত দেওয়া হলো।

9----
app.get("/", (req, res) => {
  return res.json({ status: "All Good!" });
});
👉 হেলথ চেক রুট।

10----
app.listen(8000, () => console.log(`Server started on PORT:${8000}`));
👉 সার্ভার 8000 পোর্টে চালু হলো।


<---------worker.js file code explanation---------->
import dotenv from "dotenv";
import { Worker } from "bullmq";
import { OpenAIEmbeddings } from "@langchain/openai";
import { QdrantVectorStore } from "@langchain/qdrant";
import { PDFLoader } from "@langchain/community/document_loaders/fs/pdf";
import { CharacterTextSplitter } from "@langchain/textsplitters";
👉 এখানে আবার সব dependency ইম্পোর্ট করা হয়েছে।
PDFLoader → PDF থেকে টেক্সট পড়ার জন্য
CharacterTextSplitter → টেক্সটকে ছোট ছোট অংশে ভাগ করার জন্য

const worker = new Worker(
  "file-upload-queue",
  async (job) => {
    const data = JSON.parse(job.data);
    console.log("Job data path:", data.path);
  })
  👉 Worker তৈরি হলো। Queue (file-upload-queue) থেকে জব আসলেই Worker চালু হবে।
👉 আপলোড করা ফাইলের path লগ করা হলো।

    const loader = new PDFLoader(data.path);
    const docs = await loader.load();
👉 PDF ফাইল লোড করে টেক্সট এক্সট্র্যাক্ট করা হলো।

    const splitter = new CharacterTextSplitter({
      chunkSize: 1000,
      chunkOverlap: 200,
    });
    const splitDocs = await splitter.splitDocuments(docs);
    console.log("Total Chunks:", splitDocs.length);
👉 PDF টেক্সটকে ছোট ছোট টুকরায় ভাগ করা হলো (প্রতি চাঙ্ক ১০০০ ক্যারেক্টার, ২০০ ওভারল্যাপসহ)।

    const embeddings = new OpenAIEmbeddings({
      model: "text-embedding-3-small",
      apiKey: process.env.OPENAI_API_KEY,
    });
👉 OpenAI Embeddings তৈরি হলো (যাতে টেক্সটকে ভেক্টরে রূপান্তর করা যায়)।

    try {
      const vectorStore = await QdrantVectorStore.fromDocuments(
        splitDocs,
        embeddings,
        {
          url: "http://localhost:6333",
          collectionName: "langchainjs-testing",
        }
      );
      console.log(`All docs are added to vector store`);
    } catch (err) {
      console.error("❌ Qdrant error:", err);
    }
  ,
  {
    concurrency: 100,
    connection: {
      host: "localhost",
      port: "6379",
    },
  }

👉 প্রতিটা PDF চাঙ্ককে ভেক্টর বানিয়ে Qdrant DB তে langchainjs-testing collection এ সেভ করা হলো।
👉 Redis এর মাধ্যমে Queue ম্যানেজ হচ্ছে।




<----------------Docker-compose-yml file code explanation----------->
services:
  valkey:
    image: valkey/valkey
    ports:
      - 6379:6379

  qdrant:
    image: qdrant/qdrant
    ports:
      - 6333:6333
👉 এখানে দুইটা সার্ভিস রান হচ্ছে:
valkey → Redis-compatible in-memory DB (Queue এর জন্য দরকার)।
qdrant → Vector Database (PDF embeddings স্টোর করার জন্য দরকার)।
🔹 Docker লাগবে নাকি?

👉 এই প্রজেক্টে ৩টা জিনিস দরকার:

Express সার্ভার (index.js)
Redis / Valkey (BullMQ queue চালানোর জন্য)
Qdrant (Vector DB যেখানে embeddings রাখা হচ্ছে)
✅ Express সার্ভার তো তুমি Node দিয়ে চালাতেই পারবে।
কিন্তু — Redis (Valkey) আর Qdrant ইনস্টল করতে হবে।

👉 এখন দুইটা উপায় আছে:
Docker দিয়ে Redis + Qdrant চালাও (সহজ, এক কমান্ডে সব উঠে যাবে)।
Docker ছাড়া — Redis আর Qdrant আলাদা করে ম্যানুয়ালি ইনস্টল করতে হবে তোমার সিস্টেমে।

মানে:
Docker না থাকলেও সার্ভার চলবে, কিন্তু তোমাকে লোকাল মেশিনে Redis + Qdrant আলাদা করে চালাতে হবে।
Docker থাকলে docker compose up -d দিলেই সব সার্ভিস একসাথে চলবে।



🔹 প্রথমে Queue কেন দরকার?
তোমার সিস্টেমে ২টা আলাদা কাজ হচ্ছে:
ফাইল আপলোড (PDF ফাইল সার্ভারে সেভ করা)
ফাইল প্রসেসিং (PDF থেকে টেক্সট পড়া → চাঙ্ক করা → embeddings বানানো → Qdrant এ সেভ করা)

👉 এখন ধরো ফাইলটা বড়, ২০০+ পেজ।
যদি ইউজার /upload/pdf API কল করে → আর একই request এর ভেতরেই ফাইল প্রসেস করা হয়,
তাহলে —
API অনেক সময় নেবে (timeout হতে পারে)।
সার্ভার ব্লক হয়ে যাবে (অন্য ইউজার request হ্যান্ডেল করতে পারবে না)।

✅ তাই BullMQ queue ব্যবহার করা হয়েছে।
ফাইল আপলোড হওয়ার পর সাথে সাথে response দেওয়া হয় — "File uploaded successfully!"
আর আসল প্রসেসিং এর কাজ queue তে জব আকারে পাঠানো হয়
Worker আলাদা থ্রেডে ধীরে সুস্থে ফাইল প্রসেস করে।



🔹 Redis কেন লাগছে?
BullMQ কাজ করার জন্য একটা backend storage দরকার, যেখানে queue এর জবগুলো সেভ থাকবে।
সেটাই হলো Redis (বা Valkey)।
Redis এর কাজগুলো:
Queue তে নতুন জব যোগ হলে সেটা সেভ করে।
Worker কে বলে → “এই নাও, নতুন জব এসেছে, প্রসেস করো।”
জব complete বা fail হলে সেই স্টেট ট্র্যাক করে।
👉 মানে Redis হলো একটা “middleman” যা নিশ্চিত করে যে:
ফাইল প্রসেসিং এর জব হারিয়ে যাবে না
জবের স্টেটাস ম্যানেজ করা যাবে (pending, processing, done, failed)


এই ডায়াগ্রামে পুরো ফ্লোটা দেখানো হলো 👇
Client (User) → সার্ভারে ফাইল আপলোড/কোয়েরি পাঠায়।
Express Server (index.js) → ফাইল রিসিভ করে Queue তে পাঠায়।
Queue (BullMQ) → Redis/Valkey এর সাথে মিলে জব ম্যানেজ করে।
Redis / Valkey → Queue এর সব জব সেভ রাখে।
Worker (worker.js) → Queue থেকে জব নিয়ে ফাইল প্রসেস করে → টেক্সট ভাগ করে Embedding বানায় → Qdrant এ পাঠায়।
Qdrant DB → PDF টেক্সট embeddings সেভ করে রাখে।
Express Server → Qdrant থেকে রিলেভেন্ট কন্টেন্ট নিয়ে OpenAI এর মাধ্যমে উত্তর বানায়।
Client (User) → ফাইনাল উত্তর পায়।
